<h1>Questions</h1>

<br> <%# Just for spacing! %>

<p>Writers will post their questions here...</p>

<br>

<table>
  <thead>
    <tr>
      <th>Title</th> <%# I'M NOT SURE WHAT THIS WAS AUTOMATICALLY GENERATED FOR; SHOULDN'T IT BE A BUTTON OR SOMETHING?
      <th>Content</th> <%# Likewise... %>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @questions.each do |question| %>
      <tr>
        <td><%= question.title %></td>
        <td><%= question.content %></td>
        <td><%= link_to 'Show', question %></td>
        <td><%= link_to 'Edit', edit_question_path(question) %></td>
        <td><%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Question', new_question_path %>
