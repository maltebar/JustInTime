<%# no idea when this'll be used and for whom... %>

<%# used to have render show in _writer partial view, but then needing to place if statements into show to find out which show-view to present to which role seemed to beat purpose of partial views... %>

