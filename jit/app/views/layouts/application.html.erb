<!DOCTYPE html>
<html>
 <p class="notice"><%= notice %></p>
       <p class="alert"><%= alert %></p>
<head>

  <title>Thoughts and Questions</title> <%# Internet tab title %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<body>
    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container-fluid">
        <div class="col-md-12">



        <div class="navbar-header">
          <a class="navbar-brand"><bold>Thoughts and Questions</bold>
            <h6 class="top-bar-title">An Application to Support Just-in-Time Teaching</h6></a>
     
        </div>



        <nav> <%# navigation bar %>
          <ul class="nav navbar-nav navbar-right"> 

              <li><a class="topbar-link" >Role: 
              <%=if current_user.nil?
                    "not logged in"
                  elsif current_user.admin?
                    "Administrator"
                  else
                    if current_user.group.writer?
                      "Writer"
                    else
                      "Promoter"
                    end
                  end 
                  %></a></li> 

            <li><a class="topbar-link">Logged in as: <%= 
            if current_user.nil?
              "not logged in"
              else
                current_user.name 
            end %></a></li>

            <li><a class="topbar-link" href="<%= root_path %>"><%= 'Home' %></a></li>
            
            <li><a class="topbar-link" <%=link_to 'Sign out', destroy_user_session_path, :method => :delete%></a></li>
            <br><br><br>
          </ul>
        </nav>
      </div>
    </div>
    </header>

    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-3 col-md-2 sidebar" style='position:fixed;'>
          <ul class="nav nav-sidebar">
            <br><br>
              <% if !current_user.nil?
                  if current_user.admin? %>
                    <li><%= link_to "List of users", users_path, :class=>'sidebar-link'%>
                    <li><%= link_to "List of assignments", assignments_path, :class=>'sidebar-link'%>

                 <% else %>
                    <li><%= link_to "Homework statistics", users_path, :class=>'sidebar-link'%>
                    <li><%= link_to "Assignment", questions_path, :class=>'sidebar-link'%>
                <% end %>
              <%  end %>

                      

          </ul>
        </div>
        
      </div>
</div>

<div class="container">
  <div class="row">
      <div class="col-md-12">
    <div style="margin-left:15%">

	<%= yield %>

</div>

</body>
</html>
